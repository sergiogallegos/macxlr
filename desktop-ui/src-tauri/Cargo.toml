[package]
name = "macxlr-desktop"
version = "0.1.0"
description = "Native macOS shell for the MacXLR web UI."
authors = ["Sergio Gallegos"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
anyhow = "1.0.100"
serde = { version = "1.0.228", features = ["derive"] }
tauri = { version = "2.0.0", features = [] }

[workspace]
